---
title: Label customization
order: 70
tags: [labelProps, descriptionProps, customization, slot-props]
highlightLines: []
status: stable
since: 1.0.0
hidden: false
---

Each `Stepper.Step` accepts `labelProps` and `descriptionProps` — any `<Text>` props (`ff`, `weight`, `tracking`, `uppercase`, `size`, `colorVariant`) are applied to that step's title and helper text without changing the step indicator.
