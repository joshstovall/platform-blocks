export const TOGGLE_BUTTON_DEFAULTS = {
  selected: false,
  size: 'md',
  variant: 'solid',
  disabled: false
} as const;

export const TOGGLE_GROUP_DEFAULTS = {
  exclusive: true,
  disabled: false,
  size: 'md',
  variant: 'solid'
} as const;