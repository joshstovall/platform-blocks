---
title: Semicircle Gauge
description: Set startAngle/endAngle to a 180° sweep for a speedometer-style gauge that fills the space.
order: 3
---
Pass `startAngle={-90}` and `endAngle={90}` for a top semicircle. The chart fits the half-sweep to the available area instead of reserving an empty bottom half.
