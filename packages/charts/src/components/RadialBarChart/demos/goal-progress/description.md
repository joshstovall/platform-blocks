---
title: Goal Progress Ring
description: A single thick ring with a center readout — ideal for one headline metric.
order: 2
---
Use a single datum with `centerLabel`/`centerSubLabel` and `showValueLabels={false}` to build a focused progress ring for one headline number.
